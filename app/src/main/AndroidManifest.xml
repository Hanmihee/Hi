<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="yalantis.com.sidemenu.sample"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.INTERNET" android:required="false"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:required="false"/>
    <!-- CAMERA -->
    <uses-permission android:name="android.permission.CAMERA" android:required="false"/>

    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <!-- device -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" android:required="false"/> <!-- id -->
    <uses-permission android:name="android.permission.SEND_SMS" android:required="false"/> <!-- sms(*) -->
    <!-- device.network -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" android:required="false"/> <!-- BSSID, SSID -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" android:required="false"/> <!-- ipV4Address, ipV6Address, isRoaming, linkSpeed, type -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" android:required="false"/>

    <!-- device.gps -->
    <uses-permission android:name="android.permission.ACCESS_GPS" android:required="false"/>
    <uses-permission android:name="android.permission.ACCESS_LOCATION" android:required="false"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:largeHeap="true">

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".living4" android:label="ling4" ></activity>
        <activity android:name=".number" android:label="number" ></activity>
        <activity android:name=".yejeol" android:label="yejeol" ></activity>
        <activity android:name=".life_transportation" android:label="life_transportation" ></activity>
        <activity android:name=".kr_culture" android:label="kr_culture" ></activity>
        <activity android:name=".calendar" android:label="calendar" ></activity>
        <activity android:name=".nearhospital" android:label="nearhospital" ></activity>
        <activity android:name=".insurance" android:label="insurance" ></activity>
        <activity android:name=".nation" android:label="nation" ></activity>
        <activity android:name=".visit" android:label="visit" ></activity>
        <activity android:name=".subway" android:label="subway"></activity>
        <activity android:name=".WebviewTest" android:label="web"></activity>

    </application>

</manifest>
